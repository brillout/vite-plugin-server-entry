{
  "name": "vite-plugin-import-build",
  "version": "0.1.3",
  "description": "Make `dist/` paths statically analysable. E.g. for bundling for Cloudflare Workers.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^15.12.2",
    "typescript": "^4.3.5",
    "vite": "^2.4.4"
  }
}
