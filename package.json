{
  "name": "vite-plugin-import-build",
  "version": "0.2.2",
  "description": "Automatically import `dist/server/`",
  "dependencies": {},
  "main": "./index.js",
  "scripts": {
    "dev": "tsc --watch",
    "build": "rm -rf dist/ && tsc",
    "// === Release ===": "",
    "prepublishOnly": "pnpm run build",
    "postpublish": " git push && git push --tags",
    "release": "npm version patch && npm publish"
  },
  "devDependencies": {
    "@types/node": "^15.12.2",
    "rollup": "^2.75.5",
    "typescript": "^4.3.5",
    "vite": "^2.4.4"
  },
  "files": [
    "dist/",
    "*.d.ts",
    "*.js"
  ],
  "license": "MIT",
  "repository": "https://github.com/brillout/vite-plugin-import-build"
}
